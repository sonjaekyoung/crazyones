import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "형제 명부" };

export default function Page() {
  return (
    <PagePlaceholder
      eyebrow="Brotherhood"
      title="형제 명부"
      line="각자의 점들이 이곳에서 연결된다."
    />
  );
}
