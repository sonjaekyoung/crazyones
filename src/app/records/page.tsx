import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "광인들의 기록" };

export default function Page() {
  return (
    <PagePlaceholder
      eyebrow="Archive of the Mad"
      title="광인들의 기록"
      line="형제들이 직접 남긴 기록은 사라지지 않는다."
    />
  );
}
