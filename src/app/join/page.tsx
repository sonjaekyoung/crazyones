import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "새로운 형제" };

export default function Page() {
  return (
    <PagePlaceholder
      eyebrow="The Next One"
      title="다음 형제는 누구인가."
      line="너도 미칠 준비가 되었는가."
    />
  );
}
