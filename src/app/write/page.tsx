import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "기록 남기기" };

export default function Page() {
  return (
    <PagePlaceholder
      eyebrow="Inscribe"
      title="오늘의 광기를 기록하라."
      line="형제만 이 자리에 글을 새길 수 있다."
    />
  );
}
